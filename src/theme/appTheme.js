import { StyleSheet } from "react-native";

export const colores = {
    primary: '#5856D6',

}

export const style = StyleSheet.create({
    titleScreen: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#000',
        margin: 10
        
    } 
});